import React from "react";

const Header = props => {
  return (
    <header className="App-header">
      <div>
        <span>logo</span>
        <span>links</span>
      </div>
    </header>
  );
};

export default Header;
